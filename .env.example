# Printer name to send via: lpr -P ${PRINTER_NAME}
PRINTER_NAME=your-printer-name

# Duplex/simplex for the print job: lpr -o sides=${PRINT_SIDES}
# two-sided-long-edge (duplex, default) | two-sided-short-edge | one-sided
PRINT_SIDES=two-sided-long-edge
